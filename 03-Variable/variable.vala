void main() {
    // In vala, You can create a variable with var syntax.
    var text = "Hello!";
    var number = 5;

    // Print text variable to console
    print(text);
    // Print number variable to console
    print(@"\n$number");
    //      ^^
    //       \n is a New line.
}