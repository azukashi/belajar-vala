void main() {
    // Create a variable named "a";
    int a = 3;

    // If, Else if, Else
    if (a == 0) {
        stdout.printf("%d\n", a);
    } else if (a > 1) {
        stdout.printf("%d\n", a);
    } else {
        stdout.printf("A is less than 0");
    }
}